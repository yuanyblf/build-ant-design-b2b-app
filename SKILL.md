---
name: build-ant-design-b2b-app
description: 使用 React、TypeScript 和 Ant Design 搭建、重构或扩展可配置的 B 端／后台应用。适用于 Codex、Claude Code 及兼容 Agent Skills 的智能体创建企业控制台、管理系统、数据看板、CRUD 页面、搜索表格、表单流程、详情页、权限导航或可复用 B 端前端基础框架；也适用于产品经理专注描述业务需求，而设计、交互、布局、组件和工程规范由统一配置控制的场景。
---

# 搭建 Ant Design B 端应用

将业务需求转换为一致的 Ant Design 功能。业务规则放在功能模块中，视觉与交互规范集中在一个可编辑的规范文件中。面向产品经理的需求、设计说明和交付文档统一使用中文。

## 分离三类关注点

1. **业务意图**：角色、目标、字段、状态、操作、权限、规则和验收标准。
2. **产品模式**：列表、详情、新建编辑、审批、看板、设置或分步流程。
3. **共享规范**：设计 Token、密度、应用骨架、表格、表单、反馈、导航和无障碍。

不要将经常变化的业务规则隐藏在公共 UI 组件中，也不要在业务页面重复定义共享 Token。

## 严格遵守规范

- 将本 Skill、按任务读取的 `references/` 和生效的 `b2b-standards.json` 视为 B 端设计与实现的强制规范。
- 既有代码、框架默认样式、Agent 习惯、示例模板或临时实现与规范冲突时，必须修改冲突实现以符合规范，不得折中、忽略或静默保留偏差。
- 只有用户明确要求修改规范本身时，才先更新规范、配置、校验和代表性 Demo，再按新规范实现；不能为迁就某个页面而绕过规范。
- 交付前逐项执行规范一致性检查；发现不符合项必须修复，无法修复时明确报告，不得将其描述为已完成。

## 每次任务的起点

1. 检查仓库、包管理器、框架、Ant Design 版本、路由、数据层、测试和已有约定。
2. 按以下顺序定位规范配置：
   - 用户明确提供的路径；
   - 仓库中的 `.b2b/b2b-standards.json`；
   - 仓库中的 `config/b2b-standards.json`；
   - 本 Skill 的 `assets/b2b-standards.json` 默认值。
3. 使用 `python3 scripts/apply_standards.py --config <配置文件> --check` 校验规范。
4. 新应用默认采用 React、TypeScript 和 Ant Design；已有项目优先沿用现有技术栈，不为匹配默认值而重构。
5. 按任务读取所需文档：
   - 同步设计 Token 或用户提到 CoDesign 时，读取 `references/design-source.md`。
   - 新建基础架构、模块、路由、数据访问或权限时，读取 `references/architecture.md`。
   - 选择和组合页面时，读取 `references/page-patterns.md`。
   - 处理表单、表格、危险操作、反馈、响应式和无障碍时，读取 `references/interaction-rules.md`。
   - 将产品描述转换为研发需求时，读取 `references/product-requirements.md`。
   - 面向产品经理规划、生成和验收业务原型时，读取 `references/product-manager-workflow.md`。
   - 在 Codex 之外的 Agent 中安装或调用时，读取 `references/agent-compatibility.md`。

## 建立功能契约

实现前从需求中提取以下信息。低风险缺失项可根据项目既有模式推导；影响较大的假设必须明确说明。

```text
功能：<业务能力>
角色与权限：<谁可以查看、新建、编辑、删除、审批、导出>
实体与字段：<名称、类型、必填、校验、展示规则>
字段元数据：<默认值、数据源、组件类型、选择方式、联动规则>
业务状态：<生命周期及允许的状态流转>
查询：<字段顺序、默认值、数据源、展示形式、必填提示、日期范围、排序、分页>
操作：<行操作、批量操作、页面操作、危险操作>
异常：<空、加载、部分失败、无权限、数据冲突>
验收：<用户可观察的场景>
```

只询问会实质改变业务行为、权限、不可逆操作或数据契约的问题。间距、组件选择、加载方式等已有规范覆盖的事项不得阻塞业务开发。

## 选择最小可用页面模式

- 菜单默认进入列表页；列表页承载查询、新增、详情、行操作和批量操作。
- 新增编辑字段不超过 6 个且简单低风险时使用弹窗；7～16 个字段或需要保留列表上下文时使用抽屉；更复杂时使用独立页面。
- 详情字段不超过 8 个且无需分组时使用弹窗；9～20 个字段、需要分组或对照列表时使用抽屉；更复杂时使用独立页面。
- 同一列表页、同一业务对象的详情与编辑必须使用相同承载方式：同时使用弹窗、同时使用抽屉或同时使用独立页面。分别评估后结果不同时，统一提升到两者中复杂度更高的方式，顺序为弹窗 < 抽屉 < 独立页面。
- 有先后顺序的任务：使用步骤流程，并按步骤校验；需要时保存草稿。
- 指标与趋势：只有用户确实需要监控或比较时才使用看板。

字段数只作为初始判断。表单必须进一步选择基础表单、分步表单或高级表单；详情必须选择基础详情页或高级详情页。包含长文本、上传、子表、关联选择、状态流转或高风险提交时至少提升一级承载方式。完整规则读取 `references/page-patterns.md`。优先组合 Ant Design 基础组件。只有至少两个真实功能共享相同行为且接口稳定时，才提取公共封装。

## 使用统一应用骨架

默认直接组合 Ant Design `Layout`、`Header`、`Sider`、`Content`、横向 `Menu` 和内联 `Menu`：

- 顶部通栏：左侧 Logo，中间为“应用”切换区，右侧当前用户及全局操作；顶部应用不计入菜单层级。
- 主体左侧：直接展示当前应用的“应用菜单”，最多支持两级，不继续增加层级；左侧栏不设置应用标题区。
- 顶部 Header 与左侧菜单均支持浅色和深色模式；业务提示未明确要求深色时，必须使用浅色模式。
- 主体右侧：面包屑、页面标题、页面操作和功能内容区。
- 应用切换后必须同步替换左侧应用菜单并进入该应用默认页；应用、菜单、默认路由和权限共用一份元数据。
- 顶部可直接展示少量常用应用；应用超过直显数量后，使用“全部应用”入口，以分类悬浮卡片展示和切换应用。
- 优先使用 Ant Design 公开布局与菜单能力，不重复开发菜单展开、选中、收起和键盘交互。

应用骨架结构固定；可配置项主要限定为颜色、字号、间距、边框、圆角、阴影和 Ant Design 组件 Token。只有用户明确提出不同信息架构时才改变骨架。

主品牌色固定为 `#2A56DE`，用于链接、可交互字段和主按钮；辅助色固定为 `#FF6B06`，仅用于特殊强调和个别标签，不替代主交互色。

## 按边界实现

功能代码保持内聚，对外只暴露必要接口。项目没有既有结构时采用：

```text
src/
  app/                 # Provider、路由、应用外壳、登录态初始化
  shared/              # 稳定公共 UI、Hooks、API 客户端、工具
  features/<功能>/
    api/               # 请求函数和 DTO
    model/             # 领域类型、校验规则、纯业务规则
    components/        # 功能内部组件
    pages/             # 路由页面组合
    routes.tsx
    index.ts
```

通过 Ant Design `ConfigProvider` 和共享布局组件应用规范。需要生成类型化配置时运行：

```bash
python3 scripts/apply_standards.py \
  --config .b2b/b2b-standards.json \
  --out-dir src/shared/design-system/generated
```

禁止直接修改生成文件；应修改 JSON 后重新生成。

## 实现要求

- 使用 TypeScript 和明确领域类型，不用 `any` 掩盖不确定性。
- 当接口 DTO 与界面模型的结构或语义不同时，将二者分开。
- 服务端状态放入项目既有服务端状态层；临时界面状态就近管理。
- 权限覆盖路由、页面操作和服务端；隐藏按钮不等于完成鉴权。
- 适合分享的筛选、排序、分页和页签状态放入 URL。
- 明确处理加载、空、失败、无权限和成功状态。
- 尽早校验；可恢复错误发生后保留用户输入。
- 危险或不可逆操作必须明确确认并说明影响。
- 页面内容区、筛选区、表格和独立表单默认流式占满可用宽度，并使用统一响应式页面留白；不得在业务页面写死整体容器宽度。表格列、弹窗、抽屉和阅读型内容可按语义设置宽度、最小宽度或最大阅读宽度。
- 按钮顺序按场景确定：弹窗／抽屉提交区使用“取消 → 保存”并整体右对齐；列表查询区使用带标准图标的“查询 → 重置 → 展开”描边按钮组并整体右对齐；表格顶部工具栏采用“只读文本居左、操作按钮组居右”，按钮组内按危险／次要到正向主动作排列。行内操作单独按“详情 → 编辑 → 复制 → 启用／禁用 → 删除”排列，删除始终最右并使用危险色。完整规则读取 `references/interaction-rules.md`。
- 列表筛选内容与表格内容左对齐；表格采用“左侧只读信息、右侧操作”，导航链接可保留在信息列，变更类动作集中在最右侧且右对齐。
- 表格默认一列对应一个明确字段，不无条件将头像、名称、邮箱等组合为复合字段；只有身份识别等明确业务场景允许组合。
- 表格根据字段内容与可用列宽自动判断溢出，超长字段显示省略号，并在 Hover 或键盘聚焦时用 Tooltip 展示全文；不得遮挡相邻字段或操作列。
- 表头所有单元格统一使用 Table `headerBg` Token；Select 使用 Ant Design 公开组件并保留规范箭头尾部间距，不使用原生下拉或内部 DOM 覆盖箭头位置。
- 表单容器自适应可用宽度，输入框与下拉框根据字段内容长度、输入预期和页面布局选择语义宽度，不将短字段控件全局拉满。
- 只使用 Ant Design 公开 API 和 Token，不依赖内部 DOM 或未公开实现。
- 保留用户已有修改，遵循项目的格式化、Lint 和测试约定。

## 交付前验证

1. 运行与改动相关的类型检查、Lint 和测试。
2. 项目包含 React／TSX 页面时，运行 `python3 scripts/check_ui_conformance.py --root <项目目录>`；任何规范违规都必须阻止交付。
3. 验证主流程以及加载、空、校验、权限和失败状态。
4. 检查键盘操作、弹窗关闭后的焦点回归、标签、对比度和窄屏溢出。
5. 确认设计值来自共享规范，不是散落的无解释字面量。
6. 对照本 Skill、相关 `references/` 和生效配置检查页面结构、响应式、按钮顺序与对齐；不允许存在未说明的规范偏差。
7. 用中文总结业务行为、假设、改动文件、验证结果和待决事项。

## 修改与同步规范

将 `assets/b2b-standards.json` 作为默认模板，将项目内副本作为产品规范的唯一配置源：

1. 复制到 `.b2b/b2b-standards.json`。
2. 修改 Token 或行为配置。
3. 使用 `--check` 校验。
4. 重新生成主题文件。
5. 检查有代表性的列表、表单、详情和危险操作流程。

`references/design-source.md` 登记的 CoDesign 项目入口是在线设计规范总源，8 个演示页是可定位的子页面。先从项目入口核对设计稿数量和版本，再按子页面逐项同步；无法读取页面内容时，不得猜测颜色、字号、间距等数值，应标记为“待同步”。新增规范时，同时更新 JSON、`scripts/apply_standards.py` 中的校验逻辑和对应中文规范文档。调整应用外壳或导航时，必须读取 `references/architecture.md` 中的“统一应用骨架”。
