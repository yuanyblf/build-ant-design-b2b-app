---
description: Ant Design B 端应用、后台页面和业务原型统一规范
globs:
  - "**/*.{ts,tsx,css,json,md}"
alwaysApply: false
---

处理 React、TypeScript、Ant Design、B 端后台或业务原型任务时，先完整读取仓库根目录 `SKILL.md` 和自动生成的 `DESIGN.md`，再按其中的任务路由读取 `references/` 文档。

使用 `assets/b2b-standards.json` 作为唯一默认配置源，使用 `scripts/apply_standards.py` 校验和生成配置，使用 `scripts/generate_design_md.py` 更新 `DESIGN.md`。不要直接修改生成文件。

既有实现、Cursor 默认建议或框架默认布局与根 Skill 冲突时，严格以根 Skill、相关引用文档和生效配置为准，主动修正冲突实现。
